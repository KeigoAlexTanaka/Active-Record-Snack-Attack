<h2>
  <%= @consumer.try(:name) || @brand.try(:name) || 'All' %> Snacks
</h2>

<ul>
  <% if @snacks.empty? %>

    No snacks :(

  <% else %>

    <% @snacks.each do |snack| %>
      <li>
        <%= link_to snack.name, snack_path(snack) %>
      </li>
    <% end %>

  <% end %>
</ul>
